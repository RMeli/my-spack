spack:
  specs:
  - dla-future@develop +cuda cuda_arch=89 +miniapps ^intel-mkl ^mpich device=ch3 netmod=tcp
  view: false
  packages:
    all:
      variants:
      - cxxstd=17
  repos:
  - ~/git/DLA-Future/spack # Path to DLA-Future Spack repo
  concretizer:
    unify: true
